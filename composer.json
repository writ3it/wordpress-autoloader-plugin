{
    "name": "writ3it/wordpress-autoloader-plugin",
    "description": "Autoloader for wordpress developers.",
    "type": "project",
    "minimum-stability": "stable",
    "require": {
        "php": "^7.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^8"
    },
    "scripts": {
        "test": [
            "phpunit --bootstrap=tests/bootstrap.php tests/"
        ]
    }
}
